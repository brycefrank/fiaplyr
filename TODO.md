# TODO

- [ ] Investigate using `sparse = TRUE` in `PostStratifiedRatioEstimator` aggregation. This would require robust post-join handling (densification/completion) to ensure correct ratios for all domain combinations, but could offer significant performance improvements for large datasets.
