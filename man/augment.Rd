% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R, R/EvalHandler.R
\name{augment}
\alias{augment}
\alias{augment,WindowHandler-method}
\title{Augment a Handler with External Data}
\usage{
augment(handler, ...)

\S4method{augment}{WindowHandler}(handler, ...)
}
\arguments{
\item{handler}{A handler object.}

\item{...}{One or more scopes describing the data to join, e.g.
\code{tree(species_ref, by = "SPCD", type = "left")}.}
}
\value{
The handler with pending augmentations queued.
}
\description{
Join external data (a local data frame or a lazy database table) onto a
specific table level of a handler. This is useful for attaching reference
information such as species common names, county names, or plot-level
covariates. Columns added via \code{augment()} become available to subsequent
\link[=transform]{transform()}, \link[=subset]{subset()}, \link[=partition]{partition()}, and
\link[=aggregate]{aggregate()} calls.
}
\details{
The target table and join are specified using the scopes
(e.g., \link[=tree]{tree()}, \link[=cond]{cond()}, etc.) The first, unnamed argument
to the helper is the data to join; named arguments configure the join:

\describe{
\item{\code{by}}{Join key(s), passed to the underlying \code{dplyr} join. A character
vector or a named character vector (e.g. \code{c("SPCD" = "code")}). If
omitted, a natural join on common columns is used.}
\item{\code{type}}{Join type: one of \code{"left"} (default), \code{"inner"}, \code{"right"},
or \code{"full"}.}
\item{\code{copy}}{Logical controlling whether a local data frame is uploaded to
the remote database. If omitted, local data is copied automatically (with
a warning) when joined against a remote table.}
}
}
\section{Methods (by class)}{
\itemize{
\item \code{augment(WindowHandler)}: Join external data onto a WindowHandler table

}}
\examples{
\dontrun{
species_ref <- data.frame(SPCD = c(1, 2), COMMON_NAME = c("Pine", "Oak"))
handler |>
  augment(tree(species_ref, by = "SPCD", type = "left")) |>
  partition(tree(COMMON_NAME))
}
}
