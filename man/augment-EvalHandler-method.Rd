% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EvalHandler.R
\name{augment,EvalHandler-method}
\alias{augment,EvalHandler-method}
\title{Augment: Join External Data onto a Handler Table}
\usage{
\S4method{augment}{EvalHandler}(handler, ...)
}
\arguments{
\item{handler}{An EvalHandler object.}

\item{...}{One or more scopes describing the data to join, e.g.
\code{tree(species_ref, by = "SPCD", type = "left")}.}
}
\value{
The handler with pending augmentations queued.
}
\description{
Attach external data (a local data frame or a lazy database table) to a
specific table level via a join. Expressions must be wrapped in the
appropriate scoping helper: \code{tree()}, \code{cond()}, \code{plot()}, \code{dwm()}, or
\code{tree_history()}. The first unnamed argument to the helper is the data to
join; named arguments \code{by}, \code{type}, and \code{copy} configure the join. Columns
added here become available to later \code{transform()}, \code{subset()},
\code{partition()}, and \code{aggregate()} calls.
}
\examples{
\dontrun{
handler <- eval_handler(con, evalid = 500601)
species_ref <- data.frame(SPCD = c(1, 2), COMMON_NAME = c("Pine", "Oak"))
handler |>
  augment(tree(species_ref, by = "SPCD")) |>
  partition(tree(COMMON_NAME))
}
}
