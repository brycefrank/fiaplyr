% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WindowHandler.R
\name{window_handler}
\alias{window_handler}
\title{Connect to a Spatial Window}
\usage{
window_handler(
  db,
  geometry = NULL,
  crs = NULL,
  bbox = NULL,
  invyrs = NULL,
  statecd = NULL,
  countycd = NULL,
  county = NULL,
  backend = NULL
)
}
\arguments{
\item{db}{A DBIConnection object.}

\item{geometry}{An \code{sf} or \code{sfc} geometry (point, line, or polygon) defining
the window. Plots are selected via an R-side intersection of their
coordinates against this geometry. Requires the \code{sf} package.}

\item{crs}{The EPSG code of \code{geometry} (or \code{bbox}). If \code{NULL} and
\code{geometry} carries a coordinate reference system, it is used.}

\item{bbox}{A numeric vector of length 4 giving \code{c(xmin, ymin, xmax, ymax)}
in the coordinate system of the plot coordinates (longitude/latitude for
the default public fuzzed coordinates).}

\item{invyrs}{A numeric vector of inventory years to retain. Defaults to all
years.}

\item{statecd}{A numeric vector of state codes. When used alone, retains all
plots in those states.}

\item{countycd}{A numeric vector of county codes. Only valid in combination
with a single-valued \code{statecd} (county codes are ambiguous across states).}

\item{county}{A data frame with \code{STATECD} and \code{COUNTYCD} columns, one row per
county. Use this when selecting counties across multiple states.}

\item{backend}{An optional \link[=database_mapping]{database_mapping()} for custom
schema/table names.}
}
\value{
A \link[=WindowHandler-class]{WindowHandler} connected to the database with
the plot query restricted to the requested window.
}
\description{
Subsets the \code{PLOT} table to a spatial and temporal window. At least one of
\code{geometry}, \code{bbox}, \code{statecd}, \code{county}, or \code{countycd} should be supplied to
meaningfully restrict the plots; \code{invyrs} further restricts by inventory
year. The resulting \link[=WindowHandler-class]{WindowHandler} carries a lazy plot
query that can be retrieved with \link[=coordinates]{coordinates()} and refined
with the standard handler verbs.
}
\examples{
\dontrun{
con <- DBI::dbConnect(duckdb::duckdb(), fiadb_vt_mini_path())

# Whole states
window_handler(con, statecd = 50)

# One state, several counties
window_handler(con, statecd = 50, countycd = c(5, 1, 3))

# Counties across states
counties <- data.frame(STATECD = c(50, 50, 9), COUNTYCD = c(5, 1, 3))
window_handler(con, county = counties)

# Spatial window
win <- sf::st_read("windham.shp") # in a projected CRS
window_handler(con, geometry = win, crs = 26918)

# With a temporal range
window_handler(con, statecd = 50, invyrs = 2008:2012)
}
}
