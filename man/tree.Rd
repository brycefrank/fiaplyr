% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoped-helpers.R
\name{tree}
\alias{tree}
\title{Scoped Helper for Tree-Level Expressions}
\usage{
tree(...)
}
\arguments{
\item{...}{Zero or more named or unnamed expressions.}
}
\value{
A list of quosures tagged with \code{target_table = "tree"}.
}
\description{
Captures one or more expressions and tags them to be applied at the tree
table level during lazy evaluation. Used with \code{transform()}, \code{subset()},
or \code{partition()} to explicitly scope mutations, filters, or domain
variables.
}
\examples{
\dontrun{
  handler |>
    transform(tree(BA = 0.005454 * DIA^2)) |>
    subset(tree(STATUSCD == 1)) |>
    partition(tree(SPCD))
}
}
