% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinates.R
\name{coordinates}
\alias{coordinates}
\alias{coordinates,EvalHandler-method}
\alias{coordinates,WindowHandler-method}
\title{Retrieve Plot Coordinates}
\usage{
coordinates(handler, lon = "LON", lat = "LAT", as_sf = FALSE, crs = 4269, ...)

\S4method{coordinates}{EvalHandler}(handler, lon = "LON", lat = "LAT", as_sf = FALSE, crs = 4269, ...)

\S4method{coordinates}{WindowHandler}(handler, lon = "LON", lat = "LAT", as_sf = FALSE, crs = 4269, ...)
}
\arguments{
\item{handler}{A handler object (\link[=EvalHandler-class]{EvalHandler} or
\link[=WindowHandler-class]{WindowHandler}).}

\item{lon}{Column name containing longitude coordinates. Defaults to
\code{"LON"}.}

\item{lat}{Column name containing latitude coordinates. Defaults to
\code{"LAT"}.}

\item{as_sf}{Logical. If \code{TRUE}, returns an \code{sf} object with point geometry
built from the coordinate columns. Defaults to \code{FALSE}.}

\item{crs}{The EPSG code of the coordinate reference system, used when
\code{as_sf = TRUE}. Defaults to \code{4269} (NAD 83), the datum of the public fuzzed
FIA coordinates.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A \link[tibble:tibble]{tibble} with the plot identifiers \code{CN},
\code{STATECD}, \code{COUNTYCD}, \code{INVYR}, and \code{PLOT} alongside the coordinate
columns. When \code{as_sf = TRUE}, an \code{sf} object with point geometry.
}
\description{
Returns plot-level coordinates for an \link[=EvalHandler-class]{EvalHandler}. By
default, the public fuzzed coordinates \code{LON} and \code{LAT} from the \code{PLOT} table
are returned. Databases that store coordinates under different column names
can pass alternate names via \code{lon} and \code{lat}. Alternatively, custom
coordinate columns can be attached to the plot table with
\link[=augment]{augment()} and then retrieved with \code{coordinates()}:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{handler |>
  augment(plot(coords, by = "CN")) |>
  coordinates()
}\if{html}{\out{</div>}}

Any pending plot-level \link[=subset]{subset()} filters, \link[=transform]{transform()}
mutations, and \link[=augment]{augment()} joins are applied before coordinates are
retrieved, so the result reflects the current state of the handler.
}
\section{Functions}{
\itemize{
\item \code{coordinates(EvalHandler)}: Retrieve plot coordinates for an EvalHandler

\item \code{coordinates(WindowHandler)}: Retrieve plot coordinates for a WindowHandler

}}
\examples{
\dontrun{
con <- DBI::dbConnect(duckdb::duckdb(), fiadb_vt_mini_path())
handler <- eval_handler(con, evalid = 500601)

# Default public fuzzed coordinates
handler |> coordinates()

# As an sf object
handler |> coordinates(as_sf = TRUE)

# Custom coordinate columns via augment()
coords <- data.frame(
  CN = c(1, 2, 3),
  LON = c(-72.5, -72.6, -72.7),
  LAT = c(43.1, 43.0, 42.9)
)
handler |>
  augment(plot(coords, by = "CN")) |>
  coordinates()
}
}
